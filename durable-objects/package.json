{
  "name": "durable-objects",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "start": "wrangler dev --local --config wrangler.jsonc",
    "dev": "WRANGLER_LOG=debug wrangler dev --local --config wrangler.jsonc",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "wrangler": "^4.29.1"
  }
}
