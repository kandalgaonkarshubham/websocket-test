{
  "name": "websockets-worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "pnpm i && pnpm run dev",
    "dev": "wrangler dev --local --config dev.config.json --persist-to .wrangler/state --port 8787"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250812.0",
    "wrangler": "^4.27.0"
  }
}
